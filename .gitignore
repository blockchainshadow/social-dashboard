# 依赖与本地环境
node_modules/
.venv/
tools/

# 运行产物
logs/
backups/
output/
not-quite-dead-web/

# TikTok 本地采集的临时产物（YouTube 链路不依赖）
scripts/_debug*.mjs
*.log

# 采集任务文件锁（运行时目录，绝不入库）
.fetch-lock/

# 大快照走 R2（仓库只留代码、配置与头像）
data/youtube-history.json
web/data/youtube-history.json

# 系统文件
.DS_Store
